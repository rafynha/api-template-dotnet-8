receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

exporters:
  debug:
    verbosity: detailed
  otlp:
    # endpoint: jaeger:4317  # Jaeger aceita OTLP
    endpoint: tempo_server:4317  # Tempo aceita OTLP
    tls:
      insecure: true
  loki:
    endpoint: http://loki:3100/loki/api/v1/push  # Loki via HTTP
    tls:
      insecure: true
    headers:
      Content-Type: "application/x-protobuf"
  prometheus:
    endpoint: "0.0.0.0:8889"
    namespace: otel
    const_labels:
      collector: "otel-collector"

processors:
  batch:

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp, debug]  # Traces para Tempo
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [prometheus, debug]  # Apenas debug para métricas
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [loki, debug]  # Logs para Loki